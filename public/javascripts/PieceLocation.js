class PieceLocation {
  draggable = null;
  
  constructor(ref, top, left, height, width) {
    this.ref = ref;
    this.top = top;
    this.left = left;
    this.height = height;
    this.width = width;
  }
}
